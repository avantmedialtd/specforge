{
    "$schema": "../../../node_modules/@tauri-apps/cli/schema.json",
    "identifier": "default",
    "description": "Default capability granting baseline permissions to the main window and to reader windows. Reader labels are derived from the document they show (see crates/specforge/src/reader.rs), so they are matched by glob rather than listed: without this a reader window opens with every invoke refused, rendering nothing and reporting an error that does not name the cause.",
    "windows": [
        "main",
        "reader-*"
    ],
    "permissions": [
        "core:default",
        "core:window:allow-start-dragging",
        "core:window:allow-close",
        "notification:default",
        "autostart:default",
        "dialog:default"
    ]
}
