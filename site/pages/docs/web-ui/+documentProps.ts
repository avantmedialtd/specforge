import type { SiteDocumentProps } from '../../../site-kit/postMeta';

const documentProps: SiteDocumentProps = {
    title: 'Web UI & remote access',
    description:
        'Serving SpecForge in a browser: the embedded loopback server, the standalone specforge-serve binary, Tailscale Serve, and what --bind trades away.',
    path: '/docs/web-ui',
    modified: '2026-08-14',
};

export default documentProps;
