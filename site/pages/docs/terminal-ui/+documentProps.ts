import type { SiteDocumentProps } from '../../../site-kit/postMeta';

const documentProps: SiteDocumentProps = {
    title: 'Terminal UI',
    description:
        'The SpecForge terminal UI: install and clear quarantine on macOS, the three run modes, the interactive keys, and the five screens.',
    path: '/docs/terminal-ui',
    modified: '2026-08-14',
};

export default documentProps;
