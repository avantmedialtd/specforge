# Tasks — Harden Temporarily-Disabled Workspaces

## 1. The tray becomes one attention surface

- [ ] 1.1 Add a private row-exclusion helper to `crates/openspec-core/src/watcher.rs` (`views.iter().filter(|v| !v.is_disabled())`), documented as the single exclusion point the badge and the glyph share, and note in the comment that reading the raw cache for one of them is how a parked workspace kept flipping the menu-bar glyph in v0.16.1.
- [ ] 1.2 Rewrite `WatcherManager::any_change_touches_specs` to read the `last_views` snapshot through that helper instead of delegating to the cache, scanning `RepoView::active`'s instances and `WorkspaceView::Flat`'s changes for a non-empty `ArtifactStatus.specs` (`tray-indicator`: *Spec-Activity Glyph Variant*).
- [ ] 1.3 Rewrite `total_active_logical_count` to use the same helper so the badge and the glyph cannot drift apart again.
- [ ] 1.4 Delete `WorkspaceCache::any_change_touches_specs` from `crates/openspec-core/src/cache.rs` — it has no remaining callers and is a `pub` unfiltered twin of the predicate that just caused this defect.
- [ ] 1.5 Confirm no change is needed in `crates/specforge/src/tray.rs` or `crates/specforge/src/lib.rs`: the method name and signature are unchanged, and the startup seed at `lib.rs` runs after the synchronous cache populate — verify the initial aggregation has happened before it, or the first painted frame regresses to the default variant (`tray-indicator`: *Initial variant reflects pre-existing state at startup*).
- [ ] 1.6 Rewrite `crates/openspec-core/tests/glyph_predicate.rs` around a real registry and presentation store (its current tests build a manager with no registry, so `last_views` would stay empty). Cover: empty registry; flat and repo rows with and without spec deltas; a parked repo with a spec delta not flipping the glyph; a parked row *not* hiding an enabled row that has one; a parked flat workspace; and un-parking restoring the variant.
- [ ] 1.7 Add the badge assertion the existing suite cannot make: one enabled repository with **two** active changes asserts `total_active_logical_count() == 2`, then parking it asserts `0` — every current assertion is 0 or 1, so a `len() -> 1` mutant would otherwise survive §1.3's diff.
- [ ] 1.8 Write the `tray-indicator` delta: MODIFY *Spec-Activity Glyph Variant* in full so the variant is selected from the aggregated view and excludes disabled rows, adding scenarios for spec activity in a disabled workspace not flipping the glyph and for the flip returning on re-enable.

## 2. A parked repository keeps the identity it had while enabled

- [ ] 2.1 Add `git::main_worktree_for_common_dir(&Path) -> PathBuf` in `crates/openspec-core/src/git.rs`, reproducing `worktree.c: get_main_worktree` (strip a trailing `.git` component; otherwise return the common dir itself), replacing the private `main_worktree_path`.
- [ ] 2.2 Point `default_branch`'s third fallback step at the new helper — that call site has the same bug today, running `git branch --show-current` against a submodule's superproject.
- [ ] 2.3 Make `resolve_main_worktree`'s no-subprocess fallback in `crates/openspec-core/src/repo_view.rs` a single call to it, and amend the doc comment: it is git's own derivation, not a heuristic, and it returns what the warm `git worktree list` would — including for submodule, `--separate-git-dir`, and bare layouts (`workspace-registry`: *Cold Aggregation of Disabled Rows*).
- [ ] 2.4 Table-driven unit test in `git.rs` over all four layouts plus the `/.git`-at-root edge — this is where the `==` / `.git` / `Default::default()` mutants die, with no filesystem needed.
- [ ] 2.5 Pinning test in `git.rs` that builds a plain and a `--separate-git-dir` repository and asserts `main_worktree_for_common_dir` equals the `is_main` entry of `git::worktree_list`, so a future change in git's behaviour is caught rather than silently baked in.
- [ ] 2.6 End-to-end test in `crates/openspec-core/tests/cold_aggregation.rs`: gather a `--separate-git-dir` repository warm, then cold, and assert the same `main_worktree` and `name` — asserting the *value* (the store dir's basename), not merely equality, so a reversion to `parent()` fails on the value.
- [ ] 2.7 Confirm `no_git_is_invoked_for_a_disabled_repository` still passes: it is now the only thing killing the `delete !` mutant on the cold check, since warm and cold produce the same value for an ordinary repository.
- [ ] 2.8 Write the `workspace-registry` delta: MODIFY *Cold Aggregation of Disabled Rows* in full, replacing "using the same fallback the application already applies when git is unavailable" with an outcome requirement — repository identifier, main worktree, resulting name, and main-worktree instance tagging are the values the row carries while enabled, resolved without a git subprocess — and add the identity-unchanged scenario.

## 3. One filter, one cleanup, in the crates the gates can reach

- [ ] 3.1 Replace `get_workspace_views` in `crates/specforge/src/commands.rs` with a delegation to `AppService::workspace_views`, taking `State<'_, AppService>` and keeping the `Result<Vec<WorkspaceView>, String>` return so the JS contract is unchanged (`workspace-registry`: *Disabled Rows Excluded From the Tree Pane*).
- [ ] 3.2 Verify no `crates/specforge/src/lib.rs` change is needed: the handler entry is by path, and `list_workspaces`, `get_changes`, `get_active_count`, and `set_wsl_poll_interval_secs` still need the managed watcher and presentation handles. Run `cargo clippy` to catch any newly-unused import.
- [ ] 3.3 Record in the command's doc comment that the poison behaviour changes deliberately — a poisoned presentation mutex now degrades the tree to default labels instead of erroring, matching `specforge-web` and `specforge-tui`, and the disabled filter runs before the lock either way.
- [ ] 3.4 Add the test the desktop's new dependency lacks: `AppService::workspace_views` joins presentation overrides into surviving rows and drops parked ones (every existing display-name assertion goes through `list_workspaces`, so `join_presentation` is unpinned from this path).
- [ ] 3.5 In `AppService::remove_workspace` (`crates/openspec-app/src/service.rs`), snapshot the entry with `openspec_core::canonicalize` — the dunce-backed function the registry keys entries with — and hand that same value to `unregister`, so the entry inspected and the entry removed are provably the same. Document why std's `canonicalize` is not interchangeable (Windows verbatim forms never match a registry key).
- [ ] 3.6 Leave the `was_user_registered` gate and `presentation_keys_to_drop` untouched — see design Decision 4 for why an unconditional cascade both fails to fix this and introduces two regressions.
- [ ] 3.7 Regression test in `crates/openspec-app/tests/workspace_management.rs`: park a workspace, remove it using the spelling `std::fs::canonicalize` produces, assert the presentation store is empty, then re-register and assert the row comes back enabled and visible. Note in the test's doc comment that it can only fail-before on Windows.
- [ ] 3.8 Write the `workspace-registry` delta for the cleanup: MODIFY *Workspace Presentation Persistence* in full, adding a scenario that the entry — display name, colour, and disabled state — is removed however the unregister path spells the workspace's own path, and that re-registering starts enabled.

## 4. The terminal gets the whole feature

- [ ] 4.1 Add `disabled: bool` to `SettingsWorkspace` in `crates/specforge-tui/src/app.rs` and populate it in `refresh_settings_workspaces` from the listing field the mirror currently drops.
- [ ] 4.2 Bind `Space` on the `SettingsRow::Workspace` arm of `handle_settings_key` to a new `toggle_workspace_disabled`, following `confirm_overlay`'s shape: clone the service and sender, `tokio::spawn` the async `set_workspace_disabled`, then send `Msg::Cache`. Flip nothing locally (`terminal-ui`: *Workspace Management from the Terminal*).
- [ ] 4.3 Render the parked state in `settings_workspace_line` (`crates/specforge-tui/src/ui.rs`): dim the name and append a `(disabled)` marker after the `(missing)` marker, so a missing-and-parked row shows both.
- [ ] 4.4 Advertise the key: add `Space enable/disable` to the workspace-row footer and amend the existing Space line in the help overlay rather than adding a new one, so the fixed-size centred rect does not overflow on short terminals.
- [ ] 4.5 Add `disabled_row_count: usize` to `Model`, computed in `Model::refresh` from the **unfiltered** snapshot (`svc.watcher.workspace_views().iter().filter(|v| v.is_disabled()).count()`) — one per top-level row, so a repository registered at two worktrees counts once (`dashboard`: *Dashboard Unaffected by Workspace Disable*).
- [ ] 4.6 Render the note in the terminal Dashboard's Summary block, wording identical to the React footnote ("includes N disabled workspace(s)"), placed with the totals it qualifies rather than below the fold of a scrolling pane.
- [ ] 4.7 Leave `flatten`'s `disabled: _` discard and its comment alone — the tree still never receives a disabled row.
- [ ] 4.8 Tests in `crates/specforge-tui/src/render_tests.rs`: Space parks and un-parks through the real `Msg::Cache` path, asserting the service persisted it, the mirror reflects it, the header row leaves and returns, and `disabled_row_count` moves 0 → 1 → 0; a parked row renders `(disabled)` and the footer advertises `Space`; the Dashboard shows no note at zero, singular at one, plural at two; and `disabled_row_count == raw_views - rendered_headers`, the invariant that stops the count being re-derived from registered entries.
- [ ] 4.9 Extend the existing narrow-render smoke test with a parked workspace so the 40×12 case is covered for panics.
- [ ] 4.10 Write the `terminal-ui` delta: MODIFY *Settings Screen* and *Workspace Management from the Terminal* in full so both enumerate enable/disable alongside add, remove, rename, and recolour, with scenarios for the toggle round trip and for sibling rows moving together.

## 5. Dashboard rows stay actionable and the footnote counts rows

- [ ] 5.1 Add `pub repo_id: PathBuf` to `ShipEntry` in `crates/openspec-core/src/dashboard.rs` and set it in `repo_ships` — the struct already carries `#[serde(rename_all = "camelCase")]`, so it lands on the wire as `repoId`.
- [ ] 5.2 Extend `ships_filter_to_today_and_join_clock_and_title` with an assertion on the new field, using the repo id the fixture already sets, so the `Default::default()` mutant on that line dies.
- [ ] 5.3 Hand-mirror `repoId: string` on `ShipEntry` in `src/types.ts` in the same task — there is no codegen, and a missing mirror silently classifies every ship row as unavailable.
- [ ] 5.4 Add `src/workspaceRows.ts` with `rowKey` (mirroring `PresentationKey` as `repo:<repoId>` / `flat:<uri>`), `disabledRows`, `disabledRowCount`, and `shipRowState(entry, views, registered) -> openable | parked | unavailable`.
- [ ] 5.5 Rewrite `handleOpenShip` in `src/App.tsx` to take the `ShipEntry` and resolve by `repoId`, which also fixes the dead click for an **enabled** repository whose ship was archived in a feature worktree hosting no active change (`dashboard`: *Ship Selection Opens the Archive Browser*).
- [ ] 5.6 In `src/components/DashboardView.tsx`, mark a non-openable ship row (muted styling, a `disabled` / `unavailable` chip, an explanatory `title`) and route its click to Settings instead of swallowing it.
- [ ] 5.7 Replace `disabledCount={workspaces.filter((w) => w.disabled).length}` with `disabledRowCount(workspaces)`, and update the prop's doc comment from "registered workspaces" to top-level rows — the count of rows the tree drops.
- [ ] 5.8 Add the row styling to `src/App.css`, mirroring `.workspace-row--disabled`'s existing muted treatment rather than inventing a second one.
- [ ] 5.9 Tests in `src/workspaceRows.test.ts`: `disabledRowCount` returns 1 for two rows sharing a `repoId`, 2 for a disabled repo group plus a disabled flat workspace, 2 for two distinct disabled flat workspaces (the `null`-bucket guard), and 0 with none disabled; `shipRowState` classifies openable / parked / unavailable, including the enabled-feature-worktree case.
- [ ] 5.10 Write the `dashboard` delta: MODIFY *Dashboard Unaffected by Workspace Disable* in full — pin the resolution source for an actionable row to the registered listing rather than the filtered view, amend *Ships from a disabled workspace still appear* so selection is acknowledged and offers the way back rather than promising the archive browser, and define the note's figure as disabled top-level rows with the two-worktrees scenario.

## 6. An address into a parked workspace says so

- [ ] 6.1 Add `matchParkedSlug` to `src/routing/slug.ts`, reconstructing a parked row's slug from the registered listing — `slugify(name)` for a flat row, `slugify(basename(dirname(repoId)))` for a repository, plus the `-<shortHash(identity)>` suffixed form — with a doc comment recording that it degrades to no match for `--separate-git-dir` and bare layouts.
- [ ] 6.2 Extend `ResolveResult` in `src/routing/resolve.ts` with `{ status: "disabled"; workspaces }` and give `resolveAddress` a defaulted third parameter, threading it to the three **scope-miss** sites only — never the change-missing or artifact-missing ones (`view-routing`: *Cold-Load Address Resolution*).
- [ ] 6.3 Pass `disabledRows(workspaces)` from `src/App.tsx` and leave `addressToNodePath`'s two-argument call alone: a parked row has no tree node to reveal and it early-returns on any non-resolved status.
- [ ] 6.4 Add `src/components/DisabledAddressNotice.tsx` — names the workspace, offers `Enable <label>` and `Open Settings`, and reports a failed enable inline. No navigation follows a successful enable: the command emits `workspace-presentation-updated` and the unchanged address resolves on the next render.
- [ ] 6.5 Insert the branch before the `notFound` branch in `src/App.tsx` and soften the residual copy — with the disabled case split out, the remaining misses include a registered workspace whose change or artifact is gone, so "currently registered" is still the wrong word.
- [ ] 6.6 Tests in `src/routing/slug.test.ts` and `src/routing/resolve.test.ts`: parked flat and repo rows match on both slug forms; a Windows `C:\…\proj\.git` repo id yields `proj`; `kind` filtering never crosses flat and repo; files, artifact, and archive addresses into a parked row return `disabled`; a token matching nothing still returns `notFound`; an enabled view wins over a parked row that slugifies identically; and a resolvable workspace with a missing change still returns `notFound`.
- [ ] 6.7 Write the `view-routing` delta: MODIFY *Cold-Load Address Resolution* in full — four outcomes rather than three, and a not-found outcome that once again means the address names nothing the user has registered — with the scenario for an address into a disabled workspace.

## 7. The Settings surface tells the truth

- [ ] 7.1 Extract `prettifyError` from `src/components/SettingsView.tsx` into `src/components/errors.ts` and import it back, so it is testable without pulling React or the Tauri dialog plugin into the test process.
- [ ] 7.2 Give `WorkspaceRow` its own error state and render the failure inline with `role="alert"`, reusing the existing `.settings-error` styling, so a rejected toggle is visible in a desktop app with no console (`workspace-registry`: *Settings View*).
- [ ] 7.3 Add `siblingsOf` to `src/workspaceRows.ts` (other rows sharing a presentation key; always empty for a flat workspace) and render a note under the path when a row has siblings, naming how many folders move with the switch.
- [ ] 7.4 Extend the toggle's `title` and `aria-label` with the same scope, so it is announced and not merely shown, and leave rows without siblings unchanged — the single-worktree case must not grow chrome.
- [ ] 7.5 Add the two small rules to `src/App.css` beside the existing workspace-row styles.
- [ ] 7.6 Tests: `src/components/errors.test.ts` on the message path; `src/workspaceRows.test.ts` for `siblingsOf` — one sibling each for a two-worktree repository (self excluded by `uri`), `[]` for a single-registration repo, `[]` for a flat row even among other flat rows, and two for a three-worktree repository.
- [ ] 7.7 Write the `workspace-registry` delta for Settings: MODIFY *Settings View* in full, adding that a per-workspace control whose change cannot be persisted reports the failure and keeps showing the stored state, and that a toggle governing a repository group with more than one registered worktree makes that scope legible before use — with both scenarios.

## 8. Verification

- [ ] 8.1 Run `bun run build` first (strict `tsc --noEmit` plus the bundle) — `cargo test` fails workspace-wide in a fresh worktree until `dist/` exists, and the type-check is what proves every `onOpenShip` call site migrated to the entry signature.
- [ ] 8.2 Run `bun test` for the new and extended pure-module suites (`workspaceRows`, `errors`, `slug`, `resolve`).
- [ ] 8.3 Run the focused Rust suites: `cargo test -p openspec-core --test glyph_predicate`, `--test cold_aggregation`, `--test watcher`; `cargo test -p openspec-app --test workspace_management`; `cargo test -p specforge-tui`.
- [ ] 8.4 Run `cargo test` across the workspace and `cargo clippy` — §3.1's deletion is the most likely source of an unused import.
- [ ] 8.5 Run `git fetch origin master && git diff $(git merge-base origin/master HEAD) HEAD > /tmp/sf.diff && cargo mutants --in-diff /tmp/sf.diff` and resolve every survivor. Expect the pressure on `watcher.rs` (the shared exclusion helper and the refactored badge count), `git.rs` (the `.git` comparison), and `dashboard.rs` (the new field). Add the missing assertion rather than widening a margin; record a written reason in `.cargo/mutants.toml` only where a mutant is genuinely untestable.
- [ ] 8.6 Manual desktop smoke via `bun run wt:dev`: park a repository that has a spec delta and confirm the tray glyph reverts as well as the badge dropping; confirm the tree still renders custom names and tints after the delegation; click a parked repository's ship on the Dashboard and confirm it is marked and lands in Settings; open an address into a parked workspace and confirm the disabled notice with its one-click re-enable; register a sibling worktree and confirm the shared-scope note appears on both rows.
- [ ] 8.7 Smoke `specforge-tui`: Space on a Settings workspace row parks and un-parks it, the row shows `(disabled)`, the Browse tree loses and regains the row without a restart, and the Dashboard note appears with the right number and plural.
- [ ] 8.8 Smoke a `specforge-serve` build to confirm the web UI inherits the frontend fixes through the same `dist/` bundle and the shared accessor.
- [ ] 8.9 Restart the app and confirm the parked state, display names, and colours all survive; unregister a parked workspace and confirm re-registering it comes back enabled.
