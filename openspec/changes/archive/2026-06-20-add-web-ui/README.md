# add-web-ui

Optional local self-served web UI: a headless HTTP+SSE server wrapping AppService, reusing the existing React frontend via a transport swap. Designed to support both an embedded toggle in the desktop app and a standalone serve binary.
