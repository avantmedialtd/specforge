# Mathematical Notation Rendering — Tasks

## 1. Dependencies

- [x] 1.1 Add `katex`, `remark-math@^6`, and `rehype-katex@^7` to `package.json` dependencies via `bun add` (unified-11-compatible majors, matching `react-markdown@9`)

## 2. Frontend Implementation

- [x] 2.1 Wire dollar-math into the pipeline in `src/components/MarkdownView.tsx`: add `remarkMath` to `remarkPlugins` and `rehypeKatex` (with `throwOnError: false`, `trust: false`, `strict: "ignore"`, `errorColor: "var(--warn)"` — the token reference resolves live in KaTeX's inline error styles across every error path) to `rehypePlugins` after `rehype-highlight`; import `katex/dist/katex.min.css` once (`spec-browser`: *Mathematical Notation Rendering* — inline and display dollar syntax, code spans exempt by the parser)
- [x] 2.2 Rely on `rehype-katex`'s built-in `language-math` handling for ```math fences (it splices the whole `<pre>` out at the hast stage, before component overrides run — see design.md): delete `src/components/MathBlock.tsx`, and document near `MarkdownView.tsx`'s `pre` override why a `math` fence never reaches it (`spec-browser`: *Mathematical Notation Rendering* — math fence and its degradation; *Mermaid Diagram Rendering* / *SVG Fence Rendering* — updated special-case enumerations)
- [x] 2.3 Style in `src/App.css`, scoped under `.markdown-view`: `.katex-display { overflow-x: auto }` so wide formulas scroll inside their block; no error-colour rule needed — `KATEX_OPTIONS`' `errorColor` token reference covers every error path, including the in-math fallback spans a class selector cannot reach (`spec-browser`: *Mathematical Notation Rendering* — display overflow and quiet error indication)
- [x] 2.4 Promote a paragraph whose sole content is a standalone single-line `$$…$$` expression to display math via a local remark plugin in `MarkdownView.tsx`, attaching `mdast-util-math`'s flow-math `data` hast recipe so remark-rehype serialises it (GitHub renders the standalone single-line form as a block; embedded `$$…$$` and standalone `$…$` stay inline) (`spec-browser`: *Mathematical Notation Rendering* — display dollar syntax)

## 3. Verification

- [x] 3.1 Run `cargo test` (workspace stays green — no Rust changes expected, confirming the frontend-only boundary)
- [x] 3.2 Run `bun run build` (strict tsc + bundle; confirms plugin/type compatibility and that KaTeX assets bundle)
- [x] 3.3 Manual smoke with a scratch artifact walking the spec scenarios — performed against the identical frontend bundle served by the standalone web transport (see the *Single Frontend Bundle, Host-Detected Transport* requirement in the `web-ui` capability) with isolated app state, driven and DOM-inspected in a real browser: inline `$O(n \log n)$` typeset in prose, standalone single-line `$$…$$`, multi-line `$$` block, and ```math fence all display blocks, backticked `` `\\wsl$\Ubuntu` `` and `` `${tag}` `` staying literal, an unpaired `$` staying literal, invalid LaTeX between dollars (in-place source with `--warn` indication), an invalid ```math fence (raw source in place with the `--warn` indication), `\href{https://example.com}{x}` producing no live link and no anchor element, a wide formula scrolling inside its block without widening the pane, and math rendering in the surrounding text colour (currentColor verified in dark scheme; both error paths and text colour ride live CSS token references, the same scheme-flip machinery every other surface uses) (`spec-browser`: *Mathematical Notation Rendering* — all scenarios)
- [x] 3.4 Confirm rendered math carries MathML output alongside the visual spans — verified in the live DOM: 7 `<math>` elements each with an `annotation[encoding="application/x-tex"]` carrying the original TeX (`spec-browser`: *Mathematical Notation Rendering* — machine-readable representation)
