## 1. Shared Identifier-Chip Vocabulary

_No `openspec-core` or `specforge` work precedes these: the change is frontend-only (proposal → Impact), so the usual core → shell → frontend order starts at the frontend._

- [x] 1.1 In `src/App.css`, add the shared verbatim-identifier chip class `.ident-chip` carrying the appearance block `.row-worktree` and `.identity-branch` hold identically today — `var(--font-mono)`, `var(--text-2xs)`, `line-height: 1.5`, `var(--radius-sm)`, `padding: 0 var(--space-1)` — with `color` and `border-color` both reading `--ident-chip-ink` and falling back to their own neutrals (`--text-muted` / `--border-strong`), so the untinted chip renders exactly as it does today (design.md D3).
- [x] 1.2 In `src/App.css`, add the eight palette tint modifiers on the shared class, **one** declaration each setting `--ident-chip-ink` to `--ws-text-indigo|blue|teal|green|amber|orange|rose|purple`. Text and border cannot disagree because both read the one variable (design.md D3).
- [x] 1.3 In `src/App.css`, reduce `.row-worktree` to its layout-only properties (`flex: 0 1 auto`, `min-width: 0`, `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`) and delete the eight `.row-worktree--<colour>` rules now served by 1.2. Reduce `.identity-branch` to `flex: 0 0 auto` (design.md D1).
- [x] 1.4 Leave `.chip` / `.chip--warn` / `.chip--muted` and `.row-branch` untouched — `.chip` is the uppercase status-badge vocabulary and would corrupt a case-sensitive branch name, and `.row-branch` uses the weaker `--border` deliberately (design.md D2; proposal → Impact).
- [x] 1.5 In `src/components/WorkspaceTree.tsx`, emit the shared class alongside `row-worktree` on the tree's branch chip and move the tint modifier onto the shared class, leaving the rendered result unchanged (design.md D1).

## 2. Resolving the Owning Workspace's Colour

- [x] 2.1 In `src/changeIdentity.ts`, widen the lookup — renamed `branchForWorktree` → `branchChipForWorktree`, returning a `BranchChip { branch, color }` — so the single existing `views → kind:"repo" → active → instances` walk yields the matched instance's `branch` together with its `RepoView.color`, from one match rather than two traversals (design.md D4). Renamed because the old name no longer describes what it returns.
- [x] 2.2 Update `src/changeIdentity.test.ts`: a matched worktree yields both branch and colour; a repo with `color: null` yields a null colour rather than a derived one (`spec-browser`: *Change Identity Header in the Detail Pane* — "The branch chip stays neutral when no palette colour is configured"); the colour comes from the repo that *matched*, not the first scanned; an unmatched worktree path yields neither. 22 tests pass.
- [x] 2.3 Update every existing `branchForWorktree` call site for the new return shape — one production site (`DetailPane.tsx`) plus the test file. `tsc` runs with `noUnusedLocals`/`noUnusedParameters`, so a missed site fails `bun run build` rather than failing silently.

## 3. Tinting the Header Chip

- [x] 3.1 In `src/components/DetailPane.tsx`, replace `ChangeIdentityHeader`'s `branch: string | null` prop with the `BranchChip` the widened lookup returns, so text and tint arrive together from the same match (design.md D4).
- [x] 3.2 In `ChangeIdentityHeader`, emit `ident-chip ident-chip--<colour> identity-branch` when a colour is resolved, and `ident-chip identity-branch` when it is null (`spec-browser`: *Change Identity Header in the Detail Pane*).
- [x] 3.3 Confirm the archived path needs no colour branch of its own: `isArchivedChangeId` already suppresses the chip entirely, so no chip exists to tint and an archived change cannot be painted in a live workspace's colour (`spec-browser`: *Change Identity Header in the Detail Pane* — "An archived change shows no branch chip"; design.md D4).
- [x] 3.4 Keep the chip a sibling of `CopyableIdentity`, never a child: `.identity-name` carries `user-select: all`, so a nested chip would be swept into the copied value (`spec-browser`: *Change Identity Header in the Detail Pane* — "The copied value excludes the branch").

## 4. Verification

- [x] 4.1 `bun install && bun run build` in this worktree — required once before `cargo test` can compile at all, since `dist/` is gitignored and both `generate_context!` and specforge-web's `RustEmbed` need it.
- [x] 4.2 `cargo test` — green and unchanged; the change touches no Rust, so this proves the frontend-only scope claim rather than testing new behaviour. The mutation gate does not apply (nothing in `openspec-core` / `openspec-app` is touched).
- [x] 4.3 `bun run build` — strict `tsc` passes. `bun test src/changeIdentity.test.ts`: 22 pass.
- [x] 4.4 Visual smoke via the browser loop (`specforge-serve` on :4399 against the freshly built `dist/`, which the debug build reads from disk per request). The native shell is not under test here, so `bun tauri dev` was not needed. Verified live on this change's own proposal in the `specforge` workspace (configured **purple**): the header chip renders `ident-chip ident-chip--purple identity-branch`, computed `rgb(175,96,215)` for BOTH text and border, exactly `var(--ws-text-purple)`. Four distinct workspace tints on screen simultaneously (green / purple / indigo / rose), each matching its own workspace.
- [x] 4.5 Confirmed the tree renders unchanged, property by property rather than by eye. The tree chip naming the *same branch of the same change* as the header shows **zero** computed differences across all 13 properties (colour, all four border colours, width, style, radius, family, size, line-height, both paddings). Against master's declared values: tinted computes `--ws-text-<c>` for text *and* border (master set both explicitly); neutral computes `--text-muted` text with `--border-strong` border (master's base). Also checked with a `.tree` ancestor, so no descendant rule intervenes.
- [x] 4.6 Confirmed the contrast claim rather than assuming it: `.detail-identity` computes `background: rgb(19,23,30)`, identical to `var(--surface)` — the token the `--ws-text-*` shades were tuned against. `--ws-text-*` is defined in both the bare `:root` and the `prefers-color-scheme: dark` block, and the chip resolves whichever applies at use time, so the mechanism carries no scheme branch of its own. All eight tints verified correct on both surfaces.
- [x] 4.7 Confirmed a trusted click on the change name still selects and copies the name alone: selection is exactly `tint-identity-branch-chip`, contains no chip text, and the chip remains a sibling of the name rather than a child (`spec-browser`: *Change Identity Header in the Detail Pane* — "The copied value excludes the branch").
