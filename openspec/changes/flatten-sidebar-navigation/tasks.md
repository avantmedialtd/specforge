## 1. Selection model and the flattened tree

- [x] 1.1 In `src/types.ts`, shrink `TreeSelection` to three variants — `workspace` (flat, by uri), `repo` (by repoId), and `change` (container: `{ kind: "repo"; repoId } | { kind: "flat"; workspaceUri }`, plus the logical change name). Delete the `artifact`, `spec`, `section`, `task`, `logicalChange` and `instance` variants. Let `tsc` list every switch that breaks; each is a site in 1.2–1.5 or 4.x.
- [x] 1.2 In `src/App.tsx`, delete `scrollAnchorForSelection` and its `default: return null`. Rewrite `repoIdForSelection` and `renderTargetForSelection` over the three arms: a `change` selection resolves to an artifact render target for the **default artifact of the default instance** (design D1: first present of Proposal, Design, Tasks, first spec; main worktree if it hosts the change, else the first instance). Put both defaults in pure, exported functions.
- [x] 1.3 Unit-test the two default functions: proposal absent → design; only a spec present → that spec; nothing present → the change row still selects and the pane shows the empty state; main worktree hosts the change → main; main does not → first instance in aggregator order.
- [x] 1.4 In `src/components/WorkspaceTree.tsx`, delete `ArtifactSubtree`, `ArtifactNode`, `CapabilitySpecNode`, `SectionNode`, `TaskNode`, `InstanceNode`, `DisclosureGroup`'s instance use, `defaultIsOpenForSection`, `allTasksDone`'s tree use, and the `artifactNodeId`/`specNodeId`/`instanceId` helpers. A `LogicalChangeRow` and a `FlatChangeNode` become leaf rows (`spec-browser`: *Workspace Tree Hierarchy*).
- [x] 1.5 Render a multi-instance logical change as **one** two-line row: line 2's leading edge shows an instance-count chip (`N worktrees`) where a singleton shows its branch chip; trailing status from the default instance; no divergence label on the row (`spec-browser`: *Two-Line Sole-Change-Row Layout*, design D5). Keep the favourite toggle keyed on the logical change.
- [x] 1.6 Remove collapse persistence: drop the `collapsed`/`expanded` sets' hydration from and writes to settings, and delete the `getCollapsedTreeNodeIds`/`setCollapsedTreeNodeIds`/`getExpandedTreeNodeIds`/`setExpandedTreeNodeIds` wrappers from `src/api.ts` **without** touching their Rust handlers or dispatch arms (design D6 — the settings fields stay readable). Top-level disclosure is `useState` for the session, open by default; the transient reveal overlay stays.
- [x] 1.7 Rewrite the keyboard model for two levels: one Tab stop, ArrowUp/Down across visible rows, ArrowRight/Left open/close a top-level row or jump child↔parent, Home/End, first-letter typeahead, Enter/Space selects a change row or toggles a top-level row, debounced follow-focus opens a change row's default artifact, focus falls back to the top-level row when a focused change row disappears (`spec-browser`: *Workspace Tree Keyboard Navigation*).
- [x] 1.8 Update the `aria-level` values (1 and 2 only) and drop `aria-disabled` handling, which only dimmed absent artifact rows.

## 2. The change header: identity, instance switcher, tab strip

- [x] 2.1 In `src/components/DetailPane.tsx`, grow `ChangeIdentityHeader` into the change header (design D2): the identity row as today, then an instance-switcher row rendered only when given more than one instance, then the artifact tab strip. Export it so `ArchiveView` can place it. Keep the two-element sticky/inner structure and the macOS clearance inside the measured element (`spec-browser`: *Change Identity Header in the Detail Pane*, *Anchoring*).
- [x] 2.2 Derive the tabs from `ArtifactStatus` in a pure exported function — Proposal, Design, Tasks, then one per spec in listing order, present ones only — and unit-test it, including the empty status and a status with specs but no proposal (`spec-browser`: *Artifact Tab Strip in the Change Header*).
- [x] 2.3 Render the strip as a WAI-ARIA tablist with **manual** activation: Left/Right/Home/End move focus (roving tabindex), Enter/Space/click activate; activation calls `go()` with the artifact address, so history discipline is unchanged (design D3). The active tab is the address's artifact. The Tasks tab carries the progress meter while incomplete and the ✓ glyph when complete, from `totalTasks`/`completedTasks`.
- [x] 2.4 Render the instance switcher as a row of buttons labelled by worktree basename with the branch chip (same `identChipClass` tint as the identity chip) and the divergence label where present; mark the selected one; activation navigates to the same artifact in the chosen instance, with the scope's instance segment per *Shortest Unambiguous Address* (`spec-browser`: *Instance Switcher in the Change Header*, *Per-Instance Divergence Label*; design D4).
- [x] 2.5 In `src/App.tsx`, pass the header what it needs from `views` for the rendered target: the logical change's instances, the rendered instance, and its `ChangeData.artifacts` and task counts. Resolve these once in a memo keyed on the target, not per render.
- [x] 2.6 In `src/components/ArchiveView.tsx`, delete the `archive-header`, `archive-copy-row` and `archive-artifact-tabs` blocks of the reading branch and place the shared header in read-only mode: leading row with the `← Archive` control, the selected copy's date and the title; no chip; copies (labelled by the existing `copyLabels`) in the switcher; tabs from `archivedArtifactStatus`; the identity is the dated directory name via `changeDirectoryName` as today (`archive-browser`: *Read-Only Artifact Navigation*).
- [x] 2.7 Confirm by test that the archive's copy labels are the same strings the old `<select>` offered (the label function is unchanged; the test pins the switcher to it), and that no branch reaches the switcher for an archived change.
- [x] 2.8 Styles in `src/App.css`: the switcher row and tab strip inside `.detail-identity`, the archive's leading row, and removal of the retired `.archive-header`/`.archive-copy-*`/`.archive-artifact-tabs`/`.archive-tab` rules once nothing references them. The archive reader now takes the same macOS clearance rule as the live header — remove any archive-specific exemption.

## 3. The document outline and fragment links

- [x] 3.1 In `src/components/MarkdownView.tsx`, add heading components for `h1`–`h6` that stamp `data-line` (as `li` does) and an `id` derived by a pure `headingId(text, seen)` function implementing the common derivation — lower-case, strip punctuation except hyphens, whitespace → single hyphen, `-n` suffix for duplicates in document order (`document-outline`: *Fragment Links Resolve Within the Document*). Report `{ level, text, line, id }` for each rendered heading to the parent through a callback, in document order.
- [x] 3.2 Unit-test `headingId`: casing, punctuation (`What Changes?` → `what-changes`), duplicate suffixes in order, a heading that is only punctuation, and a heading that already ends in a digit.
- [x] 3.3 In `src/components/DocumentView.tsx`, hold the heading list, filter to levels 2–3, and render the outline beside the column: an ordered list, level-3 entries subordinate, each entry a button in Tab order that scrolls to `[data-line]` below the measured sticky header via the existing anchor path (`document-outline`: *Document Outline Surface*, *Outline Navigation*). Render nothing when the filtered list is empty.
- [x] 3.4 Track the current entry with an `IntersectionObserver` rooted at the scroll port; mark it with `aria-current`. Disconnect on unmount and on document change so no observer outlives its document.
- [x] 3.5 Add the optional `sections?: Section[]` prop; match entries by heading text; show `done/total` or the ✓ glyph per entry. Only `App` passes it, only for a live change's tasks target (`document-outline`: *Section Progress in a Tasks Outline*). Unit-test the matcher: exact match, no match, complete section.
- [x] 3.6 Placement in `src/App.css`: make the document view's root an inline-size container; seat the outline sticky in the column's trailing gutter; show it only under `@container (min-width: …)` rules written once per reading-width rung selector, none at `full` (design D8). The column's position must not change when the outline appears — verify with a before/after `getBoundingClientRect` in the smoke, not by eye.
- [x] 3.7 Make fragment-only links live: in the link interception, a `#fragment` href that matches a rendered heading id scrolls to it with no navigation and no history entry; an unmatched fragment falls into the existing dangling-link indication. Unit-test the classification (fragment matched, fragment unmatched, path-with-fragment unchanged). Update the inert-link affordance so a matched fragment link no longer renders as dead.

## 4. Routing, reveal and node paths

- [x] 4.1 In `src/routing/nodeId.ts`, return `[containerId, changeRowId]` for an artifact address and `[containerId]` for a files address; delete the instance/artifact/spec segments. Update `nodeId.test.ts`: the path-prefix collision case (a worktree id containing the main-worktree id as a substring) must still be covered at the container level.
- [x] 4.2 In `src/App.tsx`, simplify the reveal effect to open the container row transiently and select the change row; the header shows the addressed artifact and instance (`view-routing`: *Navigation Reveal Is Transient*). Confirm the reveal performs no settings write — there is no longer any API to call, so assert the wrappers are gone from `src/api.ts`.
- [x] 4.3 Update `src/routing/resolve.test.ts` where it asserted tree node paths below the change, and the `codec`/`history` tests only if a signature moved (they should not — the grammar is unchanged). *(No edit needed: `resolve.test.ts` asserts addresses and render targets only — it never named a tree node path — and the `codec`/`history` signatures are unmoved. All three pass unchanged.)*
- [x] 4.4 `ScrollAnchor` becomes `{ kind: "line"; line: number } | null` (design D9); update the three `setScrollAnchor(null)` sites and the anchor scroll in `DocumentView` to the single kind.

## 5. Documentation and cleanup

- [x] 5.1 Rewrite the **Tree selection** paragraph of `src/CLAUDE.md`: three variants, the two exhaustive switches, no scroll-anchor switch, and where the artifact/instance selection now lives (the change header).
- [x] 5.2 Grep `src/` for `sectionIndex`, `taskIndex`, `artifactNodeId`, `specNodeId`, `instanceId`, `collapsedTreeNodeIds`, `expandedTreeNodeIds`, `archive-tab` and `archive-copy-select`; every hit is either deleted or deliberately kept with a comment saying why.
- [x] 5.3 Delete CSS for retired tree depths (`.row` level indents beyond 2, task/section row styles, the dim absent-artifact treatment).

## 6. Verification

- [x] 6.1 `bun install && bun run build` (strict `tsc` + Vite) — must be green before any `cargo` command, and `dist/` must be fresh before any UI check. *(Green; `dist/` rebuilt last, after every source edit.)*
- [x] 6.2 `bun test` — new tests from 1.3, 2.2, 2.7, 3.2, 3.5, 3.7, 4.1 pass alongside the existing suite. Root discovery must still be 23 files plus the new ones; if it jumps, check `bunfig.toml` (the site's Playwright specs). *(491 pass / 0 fail across 29 files. The pre-change baseline was 423 across **24** files, not the 23 this task recorded — one test file had been added since it was written — so 24 + 5 new (`changeNavigation`, `archiveCopies`, `outline`, `links`, `api`) = 29. No site spec was collected.)*
- [x] 6.3 `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test` — no Rust changed, but `specforge-web` embeds `dist/`, and CI runs all three. *(All three green: fmt clean, clippy clean with `-D warnings`, 696 tests pass / 0 fail against the fresh `dist/`.)*
- [ ] 6.4 **NOT RUN BY THE IMPLEMENTER — session smoke, browser loop** (`specforge-serve` + the fresh `dist/`, per the project's verification notes): a repo with one active change shows workspace → change only; clicking the change opens its proposal with the tab strip; each tab navigates and Back returns; a change with only `tasks.md` opens on Tasks; a two-worktree change shows one row with the count chip and a switcher in the header that names branches and divergence; switching instance changes the address's instance segment.
- [ ] 6.5 **NOT RUN BY THE IMPLEMENTER — session smoke, archive**: open an archived change; exactly one header; back control, date and title in its leading row; copies in the switcher with the same labels as before and no branch; tabs from the copy's status; switching copy re-derives tabs.
- [ ] 6.6 **NOT RUN BY THE IMPLEMENTER — session smoke, outline**: at `default` width, a wide pane shows the outline beside the column and the column's left edge is unchanged from a document with no headings; narrowing the pane below the threshold hides it; `full` never shows it; a reader window shows it when wide; activating an entry scrolls with no history entry; a tasks document shows per-section counts; a `#fragment` link in a proposal scrolls.
- [ ] 6.7 **NOT RUN BY THE IMPLEMENTER — session smoke, native shell** (`bun tauri dev`, macOS): the archive reader's directory name is clear of the titlebar drag region — a click copies, a double-click does not zoom the window.
