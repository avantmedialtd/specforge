## 1. Core: the cache-event variant

- [ ] 1.1 In `crates/openspec-core/src/watcher.rs`, add `CacheEvent::PullRequestsUpdated` beside `QuotaUpdated`, with a doc comment naming the poller that emits it (`bitbucket-pull-requests`: *The Snapshot Is Announced on the Cache Stream*).
- [ ] 1.2 Let `cargo build --workspace` list every exhaustive match that breaks and add an explicit arm to each — no wildcard — stating what the ignore means: `crates/specforge/src/events.rs` (forwarder), `crates/specforge/src/tray.rs` (badge and glyph updaters), `crates/specforge/src/notifications.rs`, `crates/specforge-tui/src/main.rs` (`select!` mapping to `Msg::Cache`), and any TUI `--status`/`--line` path that matches the stream.

## 2. App layer: settings, HTTP posture, the poller

- [ ] 2.1 In `crates/openspec-app/src/settings.rs`, add `PanelPosition { LeftTop, LeftBottom, RightTop, RightBottom }` (`rename_all = "kebab-case"`, `Default = LeftBottom`) with a hand-written tolerant `Deserialize` on the `DocumentWidth` model, and `BitbucketConfig { enabled, username, api_token, refresh_secs (default 120), panel_position }`, all `#[serde(default)]`; add `bitbucket: BitbucketConfig` to `AppSettings` and to `impl Default` (`bitbucket-pull-requests`: *Panel Position Is a Persisted Setting*, design D2, D6).
- [ ] 2.2 Add `SettingsStore` accessors: `bitbucket_enabled()`, `set_bitbucket_enabled(bool)`, `bitbucket_refresh_secs()`, `bitbucket_panel_position()`, `set_bitbucket_panel_position(PanelPosition)`, `set_bitbucket_credentials(username, api_token)` (an empty token clears), a `pub(crate) bitbucket_credentials() -> Option<(String, String)>`, and a public `bitbucket_config_view() -> BitbucketConfigView { enabled, username, token_set, refresh_secs, panel_position }` that never carries the token (`bitbucket-pull-requests`: *Credentials Are Stored Write-Only*). Mirror `BitbucketConfigView` and `PanelPosition` by hand in `src/types.ts`.
- [ ] 2.3 Unit-test in `settings.rs`: an absent block loads disabled with `left-bottom`; an unrecognised `panelPosition` loads as `left-bottom` while a favourite and the identity beside it survive; `set_bitbucket_credentials` with an empty token clears it; `bitbucket_config_view()` serialises without an `apiToken` key.
- [ ] 2.4 In `crates/openspec-app/Cargo.toml`, add a direct `base64` edge (already in `Cargo.lock`) with a comment naming the Basic credential as the reason (design D10).
- [ ] 2.5 In `crates/openspec-app/src/usage_http.rs`, change `get(url)` to `get(url, auth: Auth)` with `Auth::Bearer(&str) | Auth::Basic { username, token }`, update the two quota callers, and extend `classify` with `Verdict::Forbidden` (403) and `Verdict::NotFound` (404); keep the quota pollers mapping both to transient. Extend the existing `classify` tests for 403/404 and add one asserting the Basic header value for a known pair (design D10).
- [ ] 2.6 Create `crates/openspec-app/src/bitbucket.rs` with the wire types `PullRequestsState { status, stale, fetched_at_unix, pull_requests, skipped_workspaces }`, `PullRequestSummary`, `ReviewSummary` and `PullRequestsStatus { Disabled, Unauthenticated, Unavailable, Ok }`, all `camelCase`, plus `PullRequestsHandle` on the `QuotaHandle` model (design D4). Mirror all four by hand in `src/types.ts`.
- [ ] 2.7 In `bitbucket.rs`, write the pure functions with tests: `build_pull_requests_url(workspace, account)` asserting `state=OPEN`, `sort=-updated_on`, `pagelen=50` and the literal `%2Bvalues.participants` with no raw `+`; `resolve_credentials(env, stored)` covering both-set, one-set and none; `account_identifier(json)` preferring `uuid` over `account_id`; `parse_pull_requests(body)` tolerating missing `participants`, `task_count`, `draft` and `links.html`; `summarize_review(pr)` excluding the author, counting pending from `reviewers`, matching by `account_id` then `uuid`, and returning `None` without `participants`; `merge_newest_first(per_workspace)` (`bitbucket-pull-requests`: *Authored Pull-Request Discovery*, *Review-State Summary*).
- [ ] 2.8 In `bitbucket.rs`, write `fetch_snapshot(credentials) -> FetchResult` running the three GETs sequentially through `usage_http`, mapping 401 anywhere to `Unauthenticated`, 403/404 on a workspace to a skipped slug, 429 to `RateLimited { retry_after }`, other failures to `Transient`, and an unparseable body to `Unavailable`; never format the token into any error string (`bitbucket-pull-requests`: *Privacy and Safety*).
- [ ] 2.9 In `bitbucket.rs`, write `run_poller` and `spawn_poller` on the `quota.rs` model: `TICK` 2 s, `MIN_REFRESH_SECS` 60, `DEFAULT_BACKOFF_SECS` 300, collapse to `Disabled` and emit once when the flag turns off, `degrade_to_stale` on transient/429, emit `CacheEvent::PullRequestsUpdated` only when the snapshot changed. Unit-test `degrade_to_stale` (keeps rows, sets `stale`; empty previous becomes `Unavailable`) (`bitbucket-pull-requests`: *Polling With Caching and Backoff*).
- [ ] 2.10 In `crates/openspec-app/src/service.rs`, add the `bitbucket: PullRequestsHandle` field built in `bootstrap`, `my_pull_requests()`, `spawn_bitbucket_poller()`, and `open_pull_request(url) -> Result<String, String>` that returns the URL only if it equals a row's `url` in the current snapshot; test the refusal and the acceptance (`bitbucket-pull-requests`: *Opening a Pull Request*, design D8).
- [ ] 2.11 In `crates/openspec-app/src/events.rs`, add `EVENT_PULL_REQUESTS_UPDATED = "pull-requests-updated"` mapped from the new variant in `event_envelope` with a null payload, and `EVENT_PULL_REQUEST_PANEL_MOVED = "pull-request-panel-moved"` with `PanelMovedPayload { position }`; add both to the module's `#[cfg(test)]` envelope assertions. Mirror the two string literals in `src/types.ts`.
- [ ] 2.12 In `crates/openspec-app/tests/wire_shape.rs`, add `PullRequestsState` (with one row whose `review` is `Some` and `fetched_at_unix` is `Some`), `BitbucketConfigView` and `PanelMovedPayload` as roots, and assert `PanelPosition`'s four kebab-case wire values in the string-enum section.
- [ ] 2.13 In `crates/CLAUDE.md`, correct the sentence naming the quota pollers as the app's only runtime network calls, list `bitbucket.rs`, and add the two event names to the `events.rs` bullet.

## 3. Desktop shell

- [ ] 3.1 In `crates/specforge/src/commands.rs`, add `get_bitbucket_config`, `set_bitbucket_enabled`, `set_bitbucket_credentials`, `set_bitbucket_panel_position` (persists, then `app.emit(EVENT_PULL_REQUEST_PANEL_MOVED, payload)`), `get_my_pull_requests`, and `open_pull_request` (calls `svc.open_pull_request` then `app.opener().open_url`) — each deserialising args and delegating, nothing more.
- [ ] 3.2 In `crates/specforge/src/lib.rs`, register the six commands in `generate_handler!` and call `svc.spawn_bitbucket_poller()` beside the two quota pollers.

## 4. Web server

- [ ] 4.1 In `crates/specforge-web/src/dispatch.rs`, add arms for `get_bitbucket_config`, `set_bitbucket_enabled`, `set_bitbucket_credentials`, `set_bitbucket_panel_position` (sending `pull-request-panel-moved` on the app-event channel, as `set_document_width` does) and `get_my_pull_requests`, with camelCase arg structs; deliberately add **no** arm for `open_pull_request` and add a test asserting it dispatches as an unknown command (`bitbucket-pull-requests`: *Opening a Pull Request*; `web-ui`: *Link Handling in the Browser Skin*).
- [ ] 4.2 Add a dispatch test that `set_bitbucket_panel_position` emits `EVENT_PULL_REQUEST_PANEL_MOVED` with the position, mirroring the existing `set_document_width` test.
- [ ] 4.3 In `crates/specforge-web/src/main.rs`, call `svc.spawn_bitbucket_poller()` beside the quota pollers so the standalone server polls.

## 5. Terminal frontend

- [ ] 5.1 In `crates/specforge-tui/src/app.rs`, set `SETTINGS_TOGGLE_COUNT` to 3, add `bitbucket_on` to `Model` (initialised from `svc.settings.bitbucket_enabled()` and refreshed where the quota mirrors are), and handle the third toggle index by calling `set_bitbucket_enabled`; in `ui.rs`, add the "BitBucket pull requests" row to the `toggles` array (`terminal-ui`: *Terminal Settings Screen*). Do not spawn the poller in `main.rs` (`bitbucket-pull-requests`: *The Terminal Frontend Does Not Render the Panel*).

## 6. Frontend: API, panel, layout, settings

- [ ] 6.1 In `src/api.ts`, add `getBitbucketConfig`, `setBitbucketEnabled`, `setBitbucketCredentials`, `setBitbucketPanelPosition`, `getMyPullRequests`, `openPullRequest`, and the listeners `onPullRequestsUpdated` and `onPullRequestPanelMoved` (typed payload), following the quota and document-width wrappers.
- [ ] 6.2 Create `src/components/PullRequestPanel.tsx` on the `QuotaPill` model: fetch on mount, re-fetch on `pull-requests-updated`, render nothing when `status === "disabled"`, a header button with title, count and chevron, the collapsed state in `localStorage` under `specforge.pullRequestsCollapsed`, and a body that is the one-line unauthenticated / unavailable / empty message or the row list; skipped workspaces in the header `title` (`bitbucket-pull-requests`: *Pull-Request Panel*).
- [ ] 6.3 In the same file, put the row model in pure exported functions and unit-test them: `reviewCellText(review | null)` (order approvals, changes requested, pending; the unknown treatment), `relativeUpdated(nowMs, updatedAtUnix)`, and `panelBodyState(snapshot)` returning `rows | unauthenticated | unavailable | empty`.
- [ ] 6.4 Rows: on the desktop a `<button>` calling `openPullRequest(url)` with failures reported quietly; in the browser skin (`isWeb()`) an `<a href target="_blank" rel="noopener noreferrer">` (`bitbucket-pull-requests`: *Opening a Pull Request*).
- [ ] 6.5 In `src/App.tsx`, hold `panelPosition` in state seeded by `getBitbucketConfig()` and updated by `onPullRequestPanelMoved`; render `<PullRequestPanel />` at exactly one of four JSX positions — before the Dashboard button's tree sibling, between `.sidebar-tree` and the Archive button, or as the first or last child of a new flex-column wrapper around `GraphRail` in the `far` slot (`spec-browser`: *Side Panes Host the Pull-Request Panel*, design D7).
- [ ] 6.6 In `src/components/SettingsView.tsx`, add a "BitBucket pull requests" section after ChatGPT quota: help copy stating a Jira/Confluence token is not accepted and naming the account, workspace-membership and pull-request read scopes with the token-creation link; the enable toggle; username and token inputs (the token input shows a "token set" placeholder when `tokenSet` and is never pre-filled) with a Save action calling `setBitbucketCredentials`; a four-way position control; the refresh interval is not exposed (`bitbucket-pull-requests`: *Credentials Are Stored Write-Only*, *Panel Position Is a Persisted Setting*).
- [ ] 6.7 In `src/App.css`, add `.pull-request-panel` styles: header row, bounded `max-height` on the body with `overflow: auto`, the stale treatment, the muted one-liner, and the far-pane flex-column wrapper so `GraphRail` keeps absorbing the remaining height; reuse the quota-strip tokens for the muted states.
- [ ] 6.8 In `src/CLAUDE.md`, add the panel's four slots and the two new events where the guide lists slots and events.

## 7. Verification

- [ ] 7.1 `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test` green (run `bun install && bun run build` first in a fresh worktree).
- [ ] 7.2 `bun run build` and `bun test` green; `tsc` reports no unmatched mirror.
- [ ] 7.3 Mutation-test the diff: `git fetch origin master && git diff $(git merge-base origin/master HEAD) HEAD > /tmp/sf.diff && cargo mutants --in-diff /tmp/sf.diff`; add assertions for any survivor in `bitbucket.rs`, `settings.rs`, `usage_http.rs` or `events.rs`, or exclude the poller loop's timing lines in `.cargo/mutants.toml` with a written reason.
- [ ] 7.4 Manual smoke with `specforge-serve` (debug build serving `dist/`, fake-HOME-isolated state) plus `bun run dev`, walking the spec scenarios: feature off leaves the sidebar byte-identical; enable with no credentials shows the unauthenticated line; set credentials (from the environment on the server, or via Settings) and confirm rows with review cells and a relative time; disable and confirm the panel disappears within one tick; move the position through all four slots and confirm a second browser tab re-seats without reload; at a short viewport with the panel expanded, assert the Settings entrypoint's bounding box is within the viewport; confirm `open_pull_request` over `/api/invoke` answers unknown command.
- [ ] 7.5 Manual smoke with `bun run wt:dev`: a row opens the PR in the system browser and the window does not navigate; hide the sidebar with Cmd+B and confirm the panel hides with it; run `specforge-tui`, confirm the third toggle row flips `bitbucket.enabled` in `settings.json` and the terminal makes no request.
